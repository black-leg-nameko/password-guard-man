# password-guard-man
Defensive password strength classifier (ML) with synthetic data generation and a local FastAPI demo. Built to be published safely: no leaked corpora, no cracking utilities, no password collection endpoints. Client‑side evaluation is strongly recommended in real deployments.
